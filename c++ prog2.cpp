#include <iostream>
using namespace std;

int main()
{
    double a,b,c;
    cout << "Enter two numbers: ";

    // Stores two floating point numbers in variable a and b respectively
    cin >> a >> b;
 
    // Performs multiplication and stores the result in variable c
	  c=a*b;  

    cout << "Product = " << c;    
    
    return 0;
}
